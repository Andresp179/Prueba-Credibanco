spring:
  datasource:
    url: jdbc:mysql://localhost:3306/credibanco
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
